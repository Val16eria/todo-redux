import React, { FC } from 'react';

import TodoList from './components/todoList';

const App: FC = () => {
    return (
        <div>
            <TodoList />
        </div>
    );
}

export default App;
